<footer>
	<div class = "cen">
		<span class="brand">The LUGVITC Blog</span>


		<div class = "links">
			<a href = "https://lugvitc.net/#/about" target="_blank">
				About us</span>
			</a>

			<a href = "https://lugvitc.net/#/contact" target="_blank">Contact us</a>
		</div>
		<p class = "copyright">
  			© 2025 LUGVITC. All rights reserved.
		</p>
	</div>
	
</footer>

<style>
	.cen{
		font-family: monospace;
		background-color: var(--footer-bg);
		color:var(--text-color);
		padding:0.4rem 1.5rem;
		display:flex;
		gap:0.5rem;
		flex-direction:column;
		border-top: 4px solid var(--header-border);
		padding-top:2rem;	
	}
	.brand{
		align-self: flex-start;
		font-family: 'Space Mono',monospace;
		font-weight: 400;
		font-size: 15px;
		letter-spacing: 0.08em;
		line-height: 1;
		color: var(--accent);
		border:1px solid var(--color-border);
		padding:10px;
		background:var(--gray);

		box-shadow:
				5px 5px 0 var(--box-shadow),
				0 0 0 1px rgba(121, 121, 121, 0.4);
		}

	.links{
		margin-left:15%;
		display: flex;
		gap: 3rem;
	}
	.links a{
		display:inline-flex;
		height: 20px;
		border:2px solid var(--color-border);
		color:black;
		font-weight:bold;
		padding:1rem 1.5rem;
		border-radius:1px;
		text-decoration:none;
		align-items:center;
		gap:0.4rem;
		line-height:1;

		font-family: 'Space Mono', monospace;
		box-shadow:
		6px 6px 0 var(--box-shadow),                 
		0 0 0 1px rgba(0,0,0,0.4); 
		transition:
		transform 0.15s ease-out,
    	box-shadow 0.15s ease-out;
	}

	a:nth-child(1){
		background:var(--color-blue);

	}
	a:nth-child(2){
		background:var(--color-green);		
	}
	a:nth-child(3){
		background:var(--color-orange);
	}
	.links a:hover{
		 transform: translate(2px, 2px);
		box-shadow:
		4px 4px 0 var(--box-shadow),
		0 0 0 1px rgba(0,0,0,0.4);
	}
	.copyright{
	    align-self: flex-end;
	}	
	
</style>