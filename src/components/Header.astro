<header>

	<div class = "left">
		<a href = "/" class="logo">
			<img src = "/luglogo.png"alt="logo">
			<span>The LUGVITC Blog</span>
		</a>
		<nav>
			<a href = "https://www.lugvitc.net" >cd ~</a>
			<a href = "/blog">cd /</a>
			<a href = "/contact">ping port : 80</a>

		</nav>
		<button aria-label = "Toggle theme"><img src="Sun.png"></button>
	</div>
	



</header>

<style>

	header{

		display:flex;
		align-items:center;
		justify-content:space-between;
		padding:0.7rem 1.3rem;
		border-bottom:1px solid #333;
		font-family:monospace;
		background-color: #000000;
		color: #eaeaea;
		
	}
	.left{
		display:flex;
		align-items:center;
		gap:12.5em;
		
	}
	.logo{
		display: flex;
		align-items: center;
		gap:1.2rem;
		text-decoration:none;
		color:inherit;
	}
	.logo img{
		height:85px;
		width:auto;
	}
	.logo span{
		font-family: 'Space Mono',monospace;
		font-weight: 400;
		font-size: 25px;
		letter-spacing: 0.08em;
		line-height: 1;
		border:2px solid #444;
		padding:15px;
		background:#3B4252;

		box-shadow:
		8px 8px 0 #636B7B,                 /* 👈 back face */
		0 0 0 1px rgba(0,0,0,0.4); 
	}
	nav{
		display:flex;
		gap:2.9rem
		
	}
	nav a{
		text-decoration:none;
		font-size:1.5rem;
		color: #eaeaea;
		padding : 1rem 1.25rem;
		border : 2px solid #444;
		border-radius: 1px;
		box-shadow:
		8px 8px 0 #636B7B,                 
		0 0 0 1px rgba(0,0,0,0.4); 
	}
	nav a:nth-child(1) {
		background: #FF7800; 
	}

	nav a:nth-child(2) {
		background: #00A9F3; 
	}

	nav a:nth-child(3) {
		background: #00C764; 
	}
	button{

		display:flex;
		align-items:center;
		border:2px solid #444;
		border-radius:1px;
		padding:10px;
		background: #FF7800;
		box-shadow:
		8px 8px 0 #636B7B,                 
		0 0 0 1px rgba(0,0,0,0.4);
	}
		.links a,
	header nav a ,button{
	transition: 
		transform 0.15s ease-out,
		box-shadow 0.15s ease-out;
	}
		.links a:hover,
		header nav a:hover ,button:hover{
	transform: translate(2px, 2px);
	box-shadow:
		4px 4px 0 #636B7B,
		0 0 0 1px rgba(0,0,0,0.4);
		}
		.links a:active,
	header nav a:active {
	transform: translate(3px, 3px);
	box-shadow:
		3px 3px 0 #636B7B,
		0 0 0 1px rgba(0,0,0,0.4);
	}

	.links a,
	header nav a,
	button {
	cursor: pointer;
	}


</style>